# FreeSwitchTitle 里程碑规划

本文档用于规划 FreeSwitchTitle 后续开发路线。当前优先目标是先完成已有功能收尾，再逐步扩展 GUI、商城、限时称号、图鉴玩法和开发者 API。

## 当前状态概览

根据 `CURRENT_STATUS.md`，当前项目已有基础能力：

- TrMenu 风格动作系统：`[console]`、`[player]`、`[op]`、`[message]`。
- 称号商城：Vault、PlayerPoints、BOTH 双货币购买。
- 权限系统：LuckPerms、GroupManager、NONE 模式。
- GUI lore 模板化：本地已实现，仍需运行时验证。
- 称号有效期基础系统：本地已实现，仍需运行时验证。
- 命令与 PlaceholderAPI：已有基础命令与 `%fst_title%`、`%fst_title_show%`、`%fst_title_uid%`、`%fst_title_count%`。

当前最重要的工作不是继续堆新功能，而是先把已有本地改动验证、整理并形成可发布版本。

---

## M0：发布前收尾与稳定化

### 目标

将当前已经实现的动作系统、商城、权限、GUI lore 模板化、称号有效期等功能稳定下来，形成一个可测试、可发布的版本。

### 功能范围

1. 配置示例收尾
   - 更新 `src/main/resources/titledata/title.yml`。
   - 默认示例使用 `actions`。
   - 保留 `commands` 兼容说明。
   - 示例覆盖佩戴、卸下、购买、权限、价格、有效期。

2. 旧字段检查
   - 检查 `equipCommands`、`unequipCommands`、`buyCommands`、`getTitleEquipCommands`、`commands`。
   - 确保旧字段只作为兼容回退存在。
   - 新逻辑统一优先读取 `actions`。

3. 构建验证
   - 运行 `./gradlew.bat build`。
   - 修复所有编译错误。

4. 基础回归验证
   - `/fst open`
   - `/fst shop`
   - `/fst list`
   - `/fst show`
   - `/fst buy`
   - `/fst add`
   - `/fst remove`
   - `/fst set`
   - `/fst clear`
   - `/fst reset`
   - `/fst reload`

5. 运行时验证
   - GUI lore 是否按状态渲染。
   - 永久称号是否不会过期。
   - 限时称号是否能自动过期。
   - 当前佩戴称号过期后是否自动卸下。
   - 过期后权限是否正确回收。
   - Vault / PlayerPoints 扣费是否安全。
   - LuckPerms / GroupManager 权限发放回收是否正常。

### 验收标准

- 插件可以成功构建。
- 默认配置可以直接使用。
- 购买、佩戴、卸下、重置流程正常。
- 权限发放失败不会破坏玩家原称号状态。
- 新旧配置兼容。
- 控制台无明显异常报错。
- 当前本地改动可提交。

### 服务端验证清单

在真实 Paper / Spigot 服务端中按以下顺序验证：

1. 启动验证
   - 插件能正常加载。
   - 配置文件能正常释放或读取。
   - 控制台无启动异常。

2. GUI 验证
   - `/fst open` 能打开玩家称号界面。
   - `/fst shop` 能打开商城界面。
   - 已拥有、未拥有、正在使用、无权限状态 lore 显示正确。
   - `{title}`、`{uid}`、`{duration}`、`{expire}`、`{vault_price}`、`{points_price}` 变量替换正确。

3. 购买验证
   - Vault 金币足够时购买成功并扣费。
   - Vault 金币不足时购买失败且不扣费。
   - PlayerPoints 点券足够时购买成功并扣点。
   - PlayerPoints 点券不足时购买失败且不扣点。
   - BOTH 双货币称号两种余额都满足时才购买成功。

4. 权限验证
   - LuckPerms 模式下佩戴称号能发放权限。
   - LuckPerms 模式下卸下称号能回收由本插件发放的权限。
   - GroupManager 模式下控制台授权 / 回收命令正确执行。
   - 切换称号失败时旧称号和旧权限不应被错误移除。

5. 有效期验证
   - `duration: permanent` 称号不会过期。
   - `duration: 10s` 测试称号能自动过期。
   - 过期称号会从拥有列表移除。
   - 当前佩戴称号过期时会自动卸下。
   - 过期后权限会正确回收。
   - 玩家重新上线时能清理离线期间过期的称号。

6. 命令回归
   - `/fst list`
   - `/fst show <uid>`
   - `/fst buy <uid>`
   - `/fst add <player> <uid>`
   - `/fst remove <player> <uid>`
   - `/fst set <player> <uid>`
   - `/fst clear <player>`
   - `/fst reset`
   - `/fst reload`

---

## M1：GUI 与配置体验完善

### 目标

让服主更容易配置 GUI，让玩家更直观地使用称号系统。

### 功能范围

1. GUI lore 模板完善
   - 支持正在使用、已拥有、未拥有、无权限、余额不足、免费领取、不可购买等状态。
   - 模板支持称号名、称号 ID、价格、货币、有效期、到期时间、状态等变量。

2. GUI 分页
   - 支持上一页、下一页、关闭、返回、当前页、总页数。
   - 称号数量超过 GUI 容量时自动分页。

3. 称号分类
   - 每个称号支持 `category`。
   - 支持全部、已拥有、可购买、充值称号、活动称号、成就称号、限定称号等分类。
   - 第一阶段先通过命令参数实现分类过滤：`/fst open <category>`、`/fst shop <category>`、`/fst list <category>`、`/fst look <player> <category>`。
   - 已新增 GUI 分类选择菜单，可在称号列表、我的称号、商城、图鉴与查看玩家称号界面中切换分类。
   - 分类图标细分与分类排序作为后续增强。

4. 消息统一配置
   - 命令提示、购买提示、错误提示、权限提示统一放入语言文件。
   - 减少代码中的硬编码文本。

5. 配置检查
   - 已新增 `/fst validate`，可检查称号加载结果、分类数量、收藏奖励引用、价格负数、空权限节点等常见问题。
   - 输出接入语言文件，支持控制台和管理员玩家查看。

### 验收标准

- GUI 显示内容基本全部可配置。
- 称号数量较多时分页正常。
- 玩家可以按分类查看称号。
- 服主可以通过配置修改大部分提示文本。
- 管理员可以通过 `/fst validate` 快速发现常见配置错误。

---

## M2：商城系统增强

### 目标

把称号购买从简单扣费升级成完整称号商城。

### 功能范围

1. `/fst balance`
   - 第一阶段已实现玩家自查 Vault / PlayerPoints 余额。
   - 已支持管理员通过 `/fst balance <player>` 查看在线玩家余额，需要 `freeswitchtitle.command.balance.other` 权限。

2. 多货币完善
   - 已支持每个称号独立配置货币类型。
   - 支持 Vault、PlayerPoints、BOTH、FREE。
   - 未配置称号级货币时回退全局 `shop.currency`。

3. 免费称号领取
   - 已支持货币类型为 FREE 的称号免费领取。
   - FREE 不检查余额、不扣费，成功后正常发放称号并执行购买动作。

4. 购买条件
   - 已支持 `requirements.permissions` 权限条件。
   - 玩家必须满足列表内全部权限才可购买。
   - 后续可扩展世界、等级、前置称号等条件。

5. 购买记录 / 操作日志
   - 已记录玩家、UUID、称号、价格、货币类型、时间、结果和来源。
   - 来源包括 GUI、命令。
   - 日志追加写入 `purchase-logs.yml`，写入失败不影响购买结果。

6. 失败保护
   - 余额不足时不扣费。
   - 发放失败时不应错误扣费。
   - 如已扣费后失败，应支持退款或明确记录异常。

### 验收标准

- 不同称号可以使用不同货币。
- 免费称号可以直接领取。
- 购买失败不会造成错误扣款。
- 管理员可通过日志排查购买与发放问题。

---

## M3：称号生命周期系统

### 目标

完善限时称号能力，支持活动、月卡、赛季、节日称号。

### 功能范围

1. 永久称号
   - 默认永久。
   - 支持 `permanent`、`forever`、`永久`、`0`。

2. 限时称号
   - 支持 `30m`、`12h`、`7d`、`30d`、`10s`、`1000ms`。
   - 无单位数字按秒解析。

3. 过期处理
   - 过期后从拥有列表移除。
   - 当前佩戴称号过期时自动卸下。
   - 正确回收权限。
   - 给玩家发送过期提示。

4. 过期动作
   - 已新增 `actions.expire`。
   - 过期时可执行消息、命令等当前动作系统已支持的动作。
   - `commands.expire` 作为旧格式兼容回退。

5. 续费功能
   - 已实现 `/fst renew <uid>`。
   - 续费复用称号当前价格和货币类型。
   - 每次续费按称号 `duration` 延长一个周期。
   - 单独续费价格和续费货币后续再扩展。

6. 限时上架
   - 已支持商城开始时间和结束时间。
   - 配置字段为 `shop.available-from` 和 `shop.available-until`。
   - GUI 商城和命令购买都会检查时间窗口。
   - 续费不受上架时间影响。

### 验收标准

- 插件重启后称号到期时间不丢失。
- 玩家上线时自动清理过期称号。
- 在线玩家周期性清理过期称号。
- 过期称号会安全卸下并回收权限。
- 永久称号不会被过期逻辑误处理。

---

## M4：动作系统增强

### 目标

让称号事件可以更灵活地联动服务器玩法。

### 功能范围

1. 新增动作节点
   - 已支持 `actions.obtain`：获得称号时执行。
   - 已支持 `actions.expire`：称号过期时执行。
   - 已支持 `actions.remove`：移除称号拥有权时执行。
   - 已支持 `actions.reset`：玩家清除当前称号时执行。

2. 新增动作类型
   - 已支持 `[broadcast]`：全服广播。
   - 已支持 `[sound]`：播放声音。
   - 已支持 `[title]`：发送屏幕标题。
   - 已支持 `[actionbar]`：发送动作栏。
   - 已支持 `[delay]`：延迟执行后续动作，支持 `20t`、`2s`、`500ms`、`1m`，纯数字按 tick 处理。
   - `[delay]` 执行后续动作前会检查玩家在线状态，玩家离线时安全停止后续动作链。

3. 变量统一
   - 已支持 `%player%`
   - 已支持 `%uuid%`
   - 已支持 `%title%`
   - 已支持 `%title_id%`
   - 已支持 `%duration%`
   - 已支持 `%expire%`
   - 已支持 `%price%`
   - 兼容支持 `{player}`、`{uuid}`、`{title}`、`{uid}`、`{title_id}`、`{duration}`、`{expire}`、`{price}`。

4. 内置粒子效果
   - 已新增独立 `particles.yml` 粒子预设配置。
   - 称号可通过 `effects.particle: <预设名>` 或 `effects.particle.preset: <预设名>` 复用预设。
   - 保留称号内联 `effects.particle` 完整配置兼容，用于少量特殊称号覆盖。
   - 佩戴称号时自动启动粒子，切换、卸下、过期、退出和插件关闭时自动停止。
   - 支持 `HALO`、`RING`、`AURA`、`SPIRAL`、`TRAIL`、`WINGS` 六种基础形状。
   - 已内置 `angel-wings` 翅膀粒子预设。
   - 可配置粒子类型、刷新间隔、数量、半径、高度、点数、速度和偏移。
   - `/fst validate` 会检查粒子预设与称号粒子配置中的粒子类型。

5. 药水与属性效果
   - 已新增独立 `effects.yml` 效果预设配置。
   - 支持 `potions` 药水效果预设和 `attributes` 属性效果预设。
   - 称号可通过 `effects.potion`、`effects.attribute` 引用预设，也支持内联配置。
   - 佩戴、上线时应用效果，切换、卸下、过期、退出和插件关闭时清理。
   - `/fst validate` 会检查药水类型和属性类型。

### 验收标准

- 新动作不破坏旧动作兼容。
- 动作执行失败不会破坏称号数据。
- 所有动作支持统一变量替换。
- 延迟动作在玩家离线时不会报错。

---

## M5：稀有度、图鉴与收藏玩法

### 目标

增加玩家收集感，让插件不只是购买和佩戴称号。

### 功能范围

1. 稀有度系统
   - 已支持 `common` 普通。
   - 已支持 `rare` 稀有。
   - 已支持 `epic` 史诗。
   - 已支持 `legendary` 传说。
   - 已支持 `limited` 限定。

2. 称号图鉴
   - 已新增 `/fst collection <category>` 图鉴入口。
   - 已拥有称号正常显示。
   - 未拥有称号显示为未收集状态。
   - 普通未解锁称号在图鉴中使用灰色占位物品显示，避免提前暴露完整称号信息。
   - 隐藏称号未解锁前不显示，已拥有后显示。

3. 收藏进度
   - 已支持显示已收集数量、总数量、完成百分比。
   - 已扩展 PlaceholderAPI：`%fst_title_collected%`、`%fst_title_total%`、`%fst_title_progress%`。

4. 收藏奖励
   - 已支持拥有指定数量称号后自动发放奖励称号。
   - 奖励阈值会记录，避免重复发放。

### 验收标准

- 玩家能查看称号图鉴。
- GUI 能显示稀有度。
- 隐藏称号不会提前暴露。
- 收藏奖励可以自动发放。

---

## M6：数据结构与迁移

### 目标

保证长期维护和高级功能扩展时，旧数据不会被破坏。

### 功能范围

1. 数据版本号
   - 已支持玩家数据 `data-version`。
   - 已支持配置结构 `config-version` 标记。

2. 新玩家数据结构
   - 保留旧字段 `title_list` 和 `using` 作为兼容主路径。
   - 已支持记录称号获得时间 `title_obtain_time_map`。
   - 已支持记录称号获得来源 `title_obtain_source_map`。
   - 已支持记录称号过期时间 `title_expire_map`。

3. 自动迁移
   - 已支持旧数据自动升级到 `data-version = 1`。
   - 已支持迁移前将旧字段快照保存到 `migration_backup_v1`。
   - 迁移失败时只输出警告，不删除旧数据。

4. 可选 SQLite
   - 当前继续使用 TabooLib PlayerDatabase，兼容本地 SQLite / MySQL 后端。
   - 独立可选 SQLite 存储层作为后续增强，暂不重复实现。

### 验收标准

- 老版本数据可以平滑升级。
- 迁移失败不会损坏原数据。
- 数据结构支持限时称号、购买记录和来源记录。

---

## M7：开发者 API 与 Bukkit 事件

### 目标

让其他插件可以安全接入 FreeSwitchTitle。

### 功能范围

1. Bukkit 事件
   - 已支持 `TitleEquipEvent`，可取消。
   - 已支持 `TitleUnequipEvent`，通知事件，不可取消。
   - 已支持 `TitleBuyEvent`，通知事件，不可取消。
   - 已支持 `TitleGrantEvent`，可取消。
   - 已支持 `TitleRemoveEvent`，可取消。
   - 已支持 `TitleExpireEvent`，通知事件，不可取消。
   - 已支持 `TitleResetEvent`，可取消。

2. 公开 API
   - 已支持获取玩家当前称号。
   - 已支持判断玩家是否拥有称号。
   - 已支持给玩家发放称号。
   - 已支持移除玩家称号。
   - 已支持设置玩家当前称号。
   - 已支持清除玩家当前称号。
   - 已保留旧 Boolean API 兼容。

3. 结果对象
   - 已新增 `TitleOperationResult`。
   - 新 result API 可返回成功 / 失败结果。
   - 失败时返回明确原因，例如称号不存在、未拥有、事件取消、权限失败、玩家离线等。

### 验收标准

- 外部插件可以安全调用 API。
- API、命令、GUI 共用同一套核心逻辑。
- 事件是否可取消有清晰规则。
- API 操作失败时有明确返回。

---

## M8：称号预览与购买确认

### 目标

让玩家在购买前安全预览称号外观，并避免误点商城物品直接扣费。

### 功能范围

1. 称号预览
   - 已新增 `/fst preview <uid>`。
   - 预览只展示粒子效果，不改变玩家当前称号。
   - 预览不发放权限，不应用药水/属性，不触发 equip 动作。
   - 支持 `preview.enable`、`preview.duration`、`preview.cooldown` 配置。
   - 预览结束和玩家退出时自动清理。

2. 购买确认 GUI
   - 已新增 `shop.confirm-purchase` 配置。
   - 商城点击称号时打开确认购买界面。
   - 点击确认后才调用购买逻辑。
   - 点击取消不会扣费。
   - 确认界面可配置标题、信息物品、确认按钮和取消按钮。

### 验收标准

- `/fst preview <uid>` 可正常预览粒子并自动结束。
- 预览不会改变玩家称号、权限、药水或属性。
- 商城购买需要二次确认。
- 取消购买不会扣费。

---

## M9：配置迁移与自动补齐

### 目标

降低老版本配置升级风险，避免旧配置缺失新增节点时导致命令或 GUI 报错。

### 功能范围

1. 配置版本
   - `config-version` 已升级到 `2`。
   - 启动和 `/fst reload` 时自动执行配置迁移检查。

2. 自动补齐
   - 自动补齐 `config.yml` 的 `shop.confirm-purchase` 和 `preview.*` 节点。
   - 自动补齐 `gui.yml` 的分类菜单、未解锁图鉴、购买确认 GUI 等节点。
   - 自动补齐 `lang/zh_CN.yml` 的新增语言项。
   - 自动生成缺失的 `particles.yml` 和 `effects.yml`。

3. 安全策略
   - 补齐前自动备份原配置。
   - 只写入缺失节点，不覆盖已有配置值。
   - 控制台输出迁移结果。

### 验收标准

- 旧配置缺失新增节点时不会导致 `/fst open` 等功能报错。
- 迁移不会覆盖服主已有配置。
- `/fst reload` 后新增节点立即生效。

---

## 推荐新功能优先级

1. GUI 分页 + 分类
   - 称号数量增加后非常必要。
   - 对玩家体验提升明显。

2. 限时称号完善
   - 适合月卡、赛季、节日、活动玩法。
   - 是称号插件很有价值的特色功能。

3. `actions.expire`
   - 与已有动作系统和有效期系统天然匹配。
   - 可以在称号过期时广播、提示、执行命令。

4. 免费称号领取
   - 适合新手称号和基础活动称号。
   - 实现成本低。

5. 购买记录 / 操作日志
   - 涉及金币、点券时非常实用。
   - 方便服主排查争议。

6. 稀有度 + 图鉴
   - 增强收集玩法。
   - 能明显提升插件完整度。

7. 开发者 API / 事件
   - 适合后期和任务、签到、活动、RPG 插件联动。

---

## 建议实际开发顺序

```text
M0 发布前收尾
  ↓
M1 GUI 与配置体验
  ↓
M2 商城系统增强
  ↓
M3 称号生命周期
  ↓
M4 动作系统增强
  ↓
M5 稀有度与图鉴
  ↓
M6 数据迁移
  ↓
M7 API 与事件
```

当前最推荐先完成：

```text
M0：发布前收尾与稳定化
```

原因：当前本地已经有 GUI lore 模板化和称号有效期基础系统，继续新增功能前，应先完成运行时验证、配置整理和提交，避免后续功能叠加导致问题难以定位。
